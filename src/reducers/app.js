// src/reducers/app.js
const initialAppState = { score: 12 };

const app = (state = initialAppState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default app;
